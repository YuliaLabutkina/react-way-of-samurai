const initialState = {
    friends: [
        { "name": "Artem", "id": 1 },
        { "name": "Illya", "id": 2 },
        { "name": "Alex", "id": 3 }
    ]
};

const sideBarReducer = (state = initialState, action) => {


    return state;
};

export default sideBarReducer;